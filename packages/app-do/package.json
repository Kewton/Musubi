{
  "name": "@musubi/app-do",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Durable Object（SQLite＋WebSocket）。cloudflare:workers を直接 import してよい唯一の場所",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty",
    "lint": "oxlint src",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@musubi/appspec-schema": "workspace:*"
  }
}
