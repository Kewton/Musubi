{
  "name": "@musubi/spec-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "L2 Spec 実行（AST上限つき評価）",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty",
    "lint": "oxlint src",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@musubi/appspec-schema": "workspace:*"
  }
}
