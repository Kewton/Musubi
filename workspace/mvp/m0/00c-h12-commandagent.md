# 🤖 00c：H-12 事前調査結果（CommandAgent 接点）

> `00-human-tasks.md` H-12 の1つ目「リポジトリの所在・アクセス権を確認」はこの調査で**完了**。
> 2つ目「ピン対象の確定」は M1 の判断事項として残る。M0 の成果物は `pins/commandagent.json`（プレースホルダ、作成済み）だけ。
> 調査日：2026-09-08

## 所在とアクセス権

| 項目 | 実測値 |
|---|---|
| リポジトリ | `github.com/Kewton/CommandAgent` |
| 可視性 | PUBLIC |
| 既定ブランチ | `main` |
| 最終更新 | 2026-09-04 |
| アクセス権 | 現在の `gh` 認証（Kewton）で読み取り可 |
| 言語・ビルド | Rust（`Cargo.toml` / `build.rs` / `justfile`） |

## ピン対象の候補（M1で確定する）

最新リリースは **プレリリース**しかない。安定タグが切られる前なので、M1着手時に再確認すること。

| タグ | commit |
|---|---|
| `v0.1.0-rc.20260720.2` | `00ba0fe37eee` |
| `v0.1.0-rc.20260720.1` | `7529ba318498` |
| `v0.1.0-rc.20260720` | `e9ec0a71a345` |

`appspec` を含むファイルはリポジトリ内に複数存在する（コード検索で21件）。スキーマの正本がどのパスかは M1 で特定する。

## M1着手時に決めること

1. **ピンの粒度** … タグ名で止めるか commit SHA で止めるか。プレリリースしか無い現状ではタグが動く可能性があるので、**SHA 推奨**。
2. **appspec-schema の正本パス**とそのバージョン表記。
3. **テンプレート tarball の配布形態**。GitHub Releases のアセットか、R2 に自前で置くか。企画書21章の境界規律（Musubi は CommandAgent を直接参照しない）に従うなら、**ビルド時にネットワーク越しで解決させない**形にする。
4. **headless契約のバージョン**とその互換性ポリシー。

> 記入先は `pins/commandagent.json`。M0 では null のまま置く。
