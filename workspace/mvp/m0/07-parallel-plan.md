# 07：M0 Issue の並列実行計画

> 対象：GitHub Issue **#2〜#33** のうち Milestone `M0` の27件。依存は各 Issue 本文から機械抽出した（2026-09-12 時点）。
> 前提：Issue の消化は**別の AI コーディングエージェント**が担当する。
> 関連：[`issues.md`](./issues.md)／[`.commandmate/profiles/musubi.json`](../../../.commandmate/profiles/musubi.json)

---

## 1. まず：🧑 の再分類（2026-09-12）

当初 🧑 が付いていた作業を精査したところ、**大半は「人間にしかできない」ではなかった**。
このリポジトリの M0 でも、H-03（R2バケット・S3キー）／H-07（main保護・Environments）／
H-08（Secrets 6件）／H-05 の予備調査／H-04 の空き調査は、すべて AI が実施している。

**「できない」理由は4種類あり、性質が違う。**

| 種類 | 内容 | 解除できるか |
|---|---|---|
| **① 規則上の禁止** | アカウント新規作成、パスワード・決済情報の入力、送金 | **できない。**能力ではなく規則。権限を渡されても実行しない |
| **② 権限の不在** | 例：API トークンを編集できるトークンが無い | **渡せば解除される** |
| **③ 監督の必要性** | 本番への apply、ロールバックの実演 | **不可逆性の問題。**能力ではない |
| **④ 責任の所在** | 名称、予算、合格の宣言 | **解除できない。**事業判断の当事者は本人 |

この整理に基づき、Issue を「AI ができる準備」と「本人にしかできない一点」に割り直した。

| Issue | 変更 |
|---|---|
| **#21** `wfp_enabled=false` の固定 | 🧑 → **🤖**。サブタスク3つのうち2つは H-13 / #27 で完了済み。残りは Terraform に1行書くだけ |
| **#24** CommandAgent 接点・ピン確定 | 🧑 → **🤖**。`Kewton/CommandAgent`（PUBLIC）への読取アクセスを確認。タグ・SHA の取得は機械作業 |
| **#19** 商標クリアランス | 候補調査と依頼文の作成を **#32（🤖）** へ分離。#19 は**発注（契約・支払い）だけ**に |
| **#20** ドメイン | ゾーン委任・Zone ID 登録・トークン権限追加・結線を **#33（🤖）** へ分離。#20 は**購入（決済）だけ**に |
| **#22 / #23** LINE / Google | **アカウント新規作成と本人確認だけ**が本人。以降は 🧑🤖 として渡せる旨を本文に明記 |
| **#28** CIトークン最小化（M3） | 能力ではなく**権限の不在**。`User API Tokens: Edit` を持つトークンを渡せば 🤖 になる（ただしそのトークンは全トークンを書き換えられる） |

**結果：🤖 が 12件 → 18件、🧑 が 6件 → 4件になった。**

### 本当に削れないのは3つだけ

1. **支払い・契約** — ドメイン購入（#20）、弁理士への発注（#19）
2. **アカウント新規作成と本人確認** — LINE（#22）、Google Cloud（#23）
3. **事業判断の宣言** — 名称の継続可否、M0 合格の判定（#18）

**本人の実作業は合計で1時間を切る。**

---

## 2. 結論：**並列度は2〜3。クリティカルパスが律速する**

| | 値 |
|---|---|
| クリティカルパス | **11段**（#2 → #3 → #5 → #6 → #8 → #9 → #10 → #15 → #16 → #17 → #18） |
| 🤖 が同時に着手できる最大 | **W0 のみ4、以降は2〜3** |
| 🧑 の残作業 | **4件・合計1時間未満**。AI の進捗をブロックしない |

27件のうち11件がクリティカルパス上にあり、所要時間はほぼ「1本の鎖を何段こなすか」で決まる。
**ワーカーを4本立てても、序盤を過ぎれば3本は待って遊ぶ。**

---

## 3. 波（M0 のみ・再分割後）

```
W0   🤖 #2  #7  #24 #32        🧑 #22 #23
W1   🤖 #3                      🧑 #19
W2   🤖 #5  #14 #21             🧑 #20      🧑🤖 #4
W3   🤖 #6  #13 #33
W4   🤖 #8  #12
W5   🤖 #9
W6   🤖 #10 #11
W7   🤖 #15 #25
W8                                          🧑🤖 #16
W9                                          🧑🤖 #17 #26
W10                                         🧑🤖 #18
```

> #27（M2）・#28（M3）は M0 スコープ外。いま着手しない。

---

## 4. 実行レーン

### レーン A — 🤖 クリティカルパス（**1本目。ここが遅れると全部遅れる**）

```
#2 → #3 → #5 → #6 → #8 → #9 → #10 → #15
```

途中に人の判断は入らない。**最優先で流す。**

### レーン B — 🤖 側道（**2本目**）

```
#7（いますぐ）→ #14（#3 後）→ #13（#5 後）→ #12（#6 後）→ #11（#9 後）→ #25（#10 後）
```

### レーン C — 🤖 独立（**3本目。序盤だけ。終わったら畳む**）

```
#24（いますぐ・CommandAgent のピン）
#32（いますぐ・弁理士候補と依頼文）※ 成果物は非公開リポジトリ側へ置く
#21（#3 後・wfp_enabled=false）
#33（#20 の購入後・ゾーン委任）
```

**レーン C は W3 までで枯れる。** それ以降は A と B の2本で足りる。

### レーン H — 🧑 本人（**いますぐ。合計1時間未満**）

```
#22 LINE Developers アカウント作成      （10分）
#23 Google Cloud アカウント・プロジェクト（10分）
#19 弁理士への発注                      （5分・#32 の完了後）
#20 ドメインの購入                      （5分・名称 N-1 の決定後）
```

**#19 と #20 は外部の待ち時間（審査・DNS 伝播）を生む。** 早く出すほど M4 直前の詰まりが減る。

### 収束点 — 🧑🤖（**無人で流さない**）

```
#4  staging/production の apply   ← 本番アカウント②に初めて資源を作る
#16 deploy-production.yml          ← 承認ゲートの実動確認
#17 ロールバックの実演             ← 本番を1つ前へ戻す
#26 試験F（無償枠の清算）
#18 M0 受入試験
```

いずれも **③監督の必要性** か **④責任の所在** に当たる。`auto_yes: false` のまま、人が見ている時だけ動かす。

---

## 5. 同時に走らせてはいけない組み合わせ（ファイル衝突）

| 衝突 | 重なるファイル | 対処 |
|---|---|---|
| **#4 × #14 × #21** | `infra/terraform/**`（#14 は `account_id` を `sensitive = true` にするため変数定義に触る。#21 は `wfp_enabled` を触る） | **W2 の3件は直列にする。** 順序は #14 → #21 → #4 |
| **#33 × #4** | `infra/terraform`（`custom_domain_enabled`） | #33 は #20 の購入待ちなので通常ぶつからない。重なったら #4 を先に |
| #13 × #14 | `.github/workflows/`、`.commandmate/verify.yaml` | 波が別（W3 / W2）。同時に動かさない |
| #10 × #25 | `infra/scripts/smoke.ts` | 依存で既に直列。並べ替えない |
| #5 × #6/#8/#9 | 各パッケージの `wrangler.jsonc` | 依存で直列。#5 が形を決めてから各 Worker |

衝突しない安全な組み合わせ：**#2×#7×#24×#32**／**#6×#13**／**#8×#12**／**#10×#11**／**#15×#25**。

> `.commandmate/profiles/musubi.json` の `scope_companions.require` に
> 「`verify.yaml` を触ったら `ci.yml` も触る（逆も）」を入れてある。
> 検証ゲートの定義が2か所で食い違うと「ローカルは通るが CI で落ちる」が起きる。

---

## 6. 最初の一手

```
🤖 ワーカー1: #2   Terraformでdev環境をゼロから再現   ← 単独のゲート。最良のワーカーを当てる
🤖 ワーカー2: #7   app-do（Durable Object）骨格
🤖 ワーカー3: #24  CommandAgent 接点・ピン対象の確定
🤖 （余力）:  #32  弁理士候補の調査と依頼文の最終化

🧑 いますぐ:  #22 LINE アカウント、#23 Google Cloud アカウント
```

**#2 が単独のゲート。** クリティカルパス11段のうち10段がその後ろに並んでいる。

---

## 7. ワーカーへ渡す前提

| | |
|---|---|
| 入口 | [`CLAUDE.md`](../../../CLAUDE.md) — 不変条件・依存の向き・public リポジトリの規律 |
| 合格の定義 | [`.commandmate/verify.yaml`](../../../.commandmate/verify.yaml) — deps / lint / typecheck / unit |
| worktree | `./infra/scripts/link-env.sh` を**最初に実行**（`.env` は追跡していないので worktree に無い） |
| ブランチ | `feat/{number}-{slug}` |
| 承認 | `auto_yes: false`。**production に触る系は人が見ている時だけ** |

**#32 だけは成果物の置き場が違う。** 商標・名称の情報は公開リポジトリに書かず、
非公開リポジトリ `Kewton/Musubi-workspace` の `legal/` へ置く。

**M0-2 以降で検証ゲートに足すもの**（対象ファイルがまだ無いため未登録）：
`terraform fmt -check` / `terraform validate`（#3）、`pnpm infra:sync --env staging --check`（#5・#13）。
足すときは `.commandmate/verify.yaml` と `.github/workflows/ci.yml` を**同時に**直すこと。
